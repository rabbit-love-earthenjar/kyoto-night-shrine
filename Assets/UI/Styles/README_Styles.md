# UI Styles

`NightShrineUITheme.asset` stores shared colors, TMP font role references, font sizes, and button scales. Assign approved TMP font assets here rather than separately on every scene object.

