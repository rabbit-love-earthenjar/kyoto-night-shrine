# Common UI Sprites

This directory is reserved for shared UI sprites after the developer approves their source and import settings.

The current common prefabs use tinted `Image` components as placeholders. Existing artwork should be assigned through the Inspector; scripts must not search for or hard-code art paths.

